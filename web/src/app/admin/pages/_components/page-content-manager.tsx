"use client";import { useMemo, useState } from "react";import { PageContent, PageContentSection } from "@/types/page-content";interface PageContentManagerProps {  slug: string;  initialContent: PageContent | null;}const FIELD = "field";const FIELD_FULL = "field field--full";function inputClass() {  return "w-full rounded-lg border border-[var(--admin-line)] bg-white px-3 py-2 text-sm";}function fieldLabel(children: React.ReactNode) {  return (    <label className="mb-1 block text-[0.72rem] font-bold uppercase tracking-wider text-[var(--admin-muted)]">      {children}    </label>  );}export default function PageContentManager({ slug, initialContent }: PageContentManagerProps) {  const [label, setLabel] = useState((initialContent as any)?.label ?? slug);  const [heroTitle, setHeroTitle] = useState((initialContent as any)?.hero.title ?? "");  const [heroSubtitle, setHeroSubtitle] = useState((initialContent as any)?.hero.subtitle ?? "");  const [sections, setSections] = useState<PageContentSection[]>((initialContent as any)?.sections ?? []);  const [saving, setSaving] = useState(false);  const [message, setMessage] = useState("");  const nextKey = useMemo(() => {    const used = new Set(sections.map((s) => s.key));    let n = sections.length + 1;    while (used.has(`section-${n}`)) n += 1;    return `section-${n}`;  }, [sections]);  const updateSection = (index: number, patch: Partial<PageContentSection>) => {    setSections((prev) => prev.map((s, i) => (i === index ? { ...s, ...patch } : s)));  };  const addSection = () => {    setSections((prev) => [...prev, { key: nextKey }]);  };  const removeSection = (index: number) => {    setSections((prev) => prev.filter((_, i) => i !== index));  };  const handleSave = async () => {    setSaving(true);    setMessage("");    try {      const res = await fetch(`/api/admin/pages/${slug}`, {        method: "PUT",        headers: { "Content-Type": "application/json" },        body: JSON.stringify({          label: label || slug,          hero: { title: heroTitle, subtitle: heroSubtitle },          sections,        }),      });      if (!res.ok) {        const err = await res.json().catch(() => ({}));        throw new Error(err.error || `Failed (HTTP ${res.status})`);      }      setMessage("Saved. The public page will refresh from cache shortly.");    } catch (err) {      setMessage(err instanceof Error ? err.message : "Save failed");    } finally {      setSaving(false);    }  };  return (    <div className="admin-panel">      <div className="admin-panel__head">        <h3>Page copy</h3>        <span className="text-[0.8rem] text-[var(--admin-muted)]">          Rendered with ISR — changes take effect after revalidation        </span>      </div>      <div className="admin-panel__body p-5">        <div className="grid gap-5" style={{ gridTemplateColumns: "repeat(auto-fit, minmax(320px, 1fr))" }}>          <div className={FIELD}>            {fieldLabel("Page label")}            <input className={inputClass()} value={label} onChange={(e) => setLabel(e.target.value)} />          </div>          <div className={FIELD_FULL}>            {fieldLabel("Hero title")}            <input className={inputClass()} value={heroTitle} onChange={(e) => setHeroTitle(e.target.value)} />          </div>          <div className={FIELD_FULL}>            {fieldLabel("Hero subtitle")}            <textarea              className={inputClass()}              rows={2}              value={heroSubtitle}              onChange={(e) => setHeroSubtitle(e.target.value)}            />          </div>        </div>        <div className="mt-6">          <div className="mb-3 flex items-center gap-3">            <h4 className="m-0 text-[0.95rem] font-bold text-[var(--admin-ink)]">Sections</h4>            <button type="button" className="admin-btn admin-btn--sm" onClick={addSection}>              + Add section            </button>          </div>          {sections.length === 0 ? (            <p className="text-[0.9rem] text-[var(--admin-muted)]">              No sections yet — add one to start editing copy blocks.            </p>          ) : (            <div className="grid gap-4">              {sections.map((section, i) => (                <div key={section.key} className="rounded-xl border border-[var(--admin-line)] p-4">                  <div className="mb-3 flex items-center justify-between gap-3">                    <span className="rounded-md bg-[var(--admin-surface-2)] px-2 py-1 font-mono text-[0.72rem] font-bold uppercase tracking-wider text-[var(--admin-brand)]">                      {section.key}                    </span>                    <button                      type="button"                      className="admin-icon-btn danger"                      aria-label="Remove section"                      onClick={() => removeSection(i)}                    >                      ×                    </button>                  </div>                  <div className="grid gap-3" style={{ gridTemplateColumns: "repeat(auto-fit, minmax(240px, 1fr))" }}>                    <div className={FIELD}>                      {fieldLabel("Eyebrow (span)")}                      <input                        className={inputClass()}                        value={section.eyebrow ?? ""}                        onChange={(e) => updateSection(i, { eyebrow: e.target.value })}                      />                    </div>                    <div className={FIELD}>                      {fieldLabel("Title (h2)")}                      <input                        className={inputClass()}                        value={section.title ?? ""}                        onChange={(e) => updateSection(i, { title: e.target.value })}                      />                    </div>                    <div className={FIELD_FULL}>                      {fieldLabel("Subtitle (p)")}                      <textarea                        className={inputClass()}                        rows={2}                        value={section.subtitle ?? ""}                        onChange={(e) => updateSection(i, { subtitle: e.target.value })}                      />                    </div>                    <div className={FIELD_FULL}>                      {fieldLabel("Paragraphs — one per line")}                      <textarea                        className={inputClass()}                        rows={4}                        value={(section.paragraphs ?? []).join("\n")}                        onChange={(e) =>                          updateSection(i, {                            paragraphs: e.target.value                              .split("\n")                              .map((l) => l.trim())                              .filter(Boolean),                          })                        }                      />                    </div>                    <div className={FIELD_FULL}>                      {fieldLabel("Checklist — one item per line")}                      <textarea                        className={inputClass()}                        rows={3}                        value={(section.checklist ?? []).join("\n")}                        onChange={(e) =>                          updateSection(i, {                            checklist: e.target.value                              .split("\n")                              .map((l) => l.trim())                              .filter(Boolean),                          })                        }                      />                    </div>                  </div>                </div>              ))}            </div>          )}        </div>        <div className="mt-6 flex items-center gap-3">          <button type="button" className="admin-btn admin-btn--primary" onClick={handleSave} disabled={saving}>            {saving ? "Saving…" : "Save changes"}          </button>          {message && (            <span className={"text-[0.85rem] " + (message.startsWith("Saved") ? "text-[var(--admin-green)]" : "text-[var(--admin-red)]")}>              {message}            </span>          )}        </div>      </div>    </div>  );}
